# Next protocol pass

Read `docs/INITIAL.md` (what ships) and `docs/ORCHESTRATE.md` (development plan). Implement the **next undone ORCHESTRATE slice** (O1–O7), not a new isolated feature. Host is Vercel.

Do not keep auto-court while adding hooks. Tick does not start court once insist exists. Court answers yes / no / human on the original proposal only.

No daemons and no `setTimeout` — time advances only in `sweep()`. Gateway never writes an objection in its own name.
